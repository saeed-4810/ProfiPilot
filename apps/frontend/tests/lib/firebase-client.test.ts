import { describe, it } from "vitest";

/**
 * Firebase client tests — placeholder until Firebase client module is implemented.
 * Vitest requires at least one test suite per file.
 */
describe("firebase-client", () => {
  it.todo("initializes Firebase app");
});
