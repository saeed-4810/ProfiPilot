import { describe, it } from "vitest";

/**
 * Login page tests — placeholder until PERF-98 (F2 Login Page) is implemented.
 * Vitest requires at least one test suite per file.
 */
describe("LoginPage", () => {
  it.todo("renders login form (PERF-98)");
});
